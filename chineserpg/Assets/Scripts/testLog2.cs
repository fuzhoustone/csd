using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testLog2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.LogWarning("testLog2 start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
