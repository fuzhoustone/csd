using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testLog : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.LogWarning("testLog start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
